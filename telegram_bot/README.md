# VK Teams Export Bot

Telegram бот для экспорта чатов из VK Teams. Создан для миграции с VK Teams на другие мессенджеры.

## Возможности

- Авторизация через корпоративную почту (код на email)
- Просмотр списка групповых и личных чатов
- Фильтрация чатов (поиск, скрытие ДР/свадеб)
- Выбор чатов для экспорта (по одному или все сразу)
- Экспорт в JSON и/или HTML формат
- HTML-экспорт в виде мини-мессенджера с поиском
- Мониторинг использования (дашборд)
- Docker-деплой

## Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/EgorAlifa/vk_teams_dump.git
cd vk_teams_dump/telegram_bot
```

### 2. Создайте Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите имя и username бота
4. Скопируйте токен

### 3. Настройте конфиг

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
TG_BOT_TOKEN=ваш_токен_от_botfather
```

### 4. Запуск

**Вариант A: Docker Compose (рекомендуется)**

```bash
docker compose up -d
```

Бот запустится на фоне. Логи:
```bash
docker logs -f vkteams_export_bot
```

**Вариант B: Локально**

```bash
pip install -r requirements.txt
python bot.py
```

## Использование бота

1. Откройте бота в Telegram
2. `/start` — приветствие
3. `/auth` — введите корпоративную почту
4. Получите код на email и введите его
5. `/chats` — список чатов
6. Выберите нужные чаты (☑️)
7. Нажмите «Экспорт» и выберите формат

## Мониторинг

При запуске через Docker Compose доступен дашборд статистики:

```
http://localhost:8080
```

Показывает:
- Активные пользователи (за час / неделю)
- Количество экспортов
- Авторизации
- Таблица последних пользователей

**API:**
- `GET /` — HTML дашборд
- `GET /api/stats` — JSON статистика
- `GET /health` — healthcheck

Если дашборд не нужен:
```bash
docker compose up -d bot  # Только бот
```

## Структура проекта

```
telegram_bot/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация
├── vkteams_client.py   # Клиент VK Teams API
├── export_formatter.py # Форматирование в JSON/HTML
├── stats.py            # Статистика (SQLite)
├── stats_server.py     # HTTP сервер дашборда
├── requirements.txt    # Зависимости
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose
├── .env.example        # Пример конфига
└── README.md           # Документация
```

## Docker

### Команды

```bash
# Запуск
docker compose up -d

# Логи бота
docker logs -f vkteams_export_bot

# Логи дашборда
docker logs -f vkteams_stats

# Остановка
docker compose down

# Пересборка после изменений
docker compose up -d --build
```

### Ресурсы

Docker Compose настроен на минимальные ресурсы:
- Бот: 1 CPU / 512 MB RAM
- Дашборд: 0.25 CPU / 64 MB RAM

Подходит для серверов 2 CPU / 4 GB RAM.

## Systemd (альтернатива Docker)

```ini
[Unit]
Description=VK Teams Export Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/telegram_bot
ExecStart=/usr/bin/python3 bot.py
Restart=always
RestartSec=10
EnvironmentFile=/path/to/telegram_bot/.env

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable vkteams-bot
sudo systemctl start vkteams-bot
```

## HTML экспорт

Экспорт в HTML создаёт интерактивный мини-мессенджер:

- Сайдбар со списком чатов
- Аватарки и превью сообщений
- Глобальный поиск (по названиям или по сообщениям)
- Поиск внутри чата с подсветкой
- Разделители по датам
- Закреплённые сообщения
- Тёмная тема (автоматически)
- Мобильная версия

## Особенности

### Скрытые чаты
Чаты с поздравлениями (ДР, свадьбы, рождение детей) автоматически скрываются, но доступны в отдельной вкладке.

### Заблокированные пользователи
Чаты с удалёнными/заблокированными пользователями показываются с иконкой 🚫 и email. Их можно экспортировать.

### Уведомления
При остановке бота активные пользователи получают уведомление о техработах.

## Поддержка

При проблемах: `e.nikonorov@goodt.me`

## Лицензия

MIT
